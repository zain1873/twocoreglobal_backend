SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=

# Email — Resend HTTP API (leave blank to use the console email backend).
# Falls back to SMTP (EMAIL_HOST/EMAIL_PORT/EMAIL_HOST_USER/EMAIL_HOST_PASSWORD)
# if RESEND_API_KEY is unset but EMAIL_HOST_USER is.
RESEND_API_KEY=
DEFAULT_FROM_EMAIL=hello@twocoreglobal.com
CONTACT_NOTIFICATION_EMAIL=hello@twocoreglobal.com
